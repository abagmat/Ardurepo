const int outPin = 9;       // вывод генератора
const int freq = 1000;      // частота

// Функция настройки при включении (однократно)
void setup()
{
  // Настроить вывод outPin на выход сигнала (OUTPUT)
  pinMode(outPin, OUTPUT);
}

//--------------------------------------------------------------------------------------------
// Главная функция - кусок текста выполняется бесконечно
void loop()
{
  tone(outPin, freq);       // включить генератор на выводе outPin с частотой freq
}
